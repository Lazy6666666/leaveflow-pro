Task statement

Use tmux-backed OMX team mode to fix the remaining type-quality issues in the current Convex + Clerk migration state.

Desired outcome

- Eliminate the meaningful remaining backend/frontend type-quality problems.
- Keep current passing verification state intact.
- Drive the team run to clean terminal completion and shutdown.

Known facts / evidence

- Live tmux leader pane is active (`$TMUX` set) and there is exactly one HUD watcher pane.
- `npx tsc --noEmit` currently passes.
- Remaining typed-wiring debt is concentrated in:
  - `convex/admin.ts`
  - `convex/lib/env.ts`
  - `convex/auth.config.ts`
  - `convex/absenceNotifications.ts`
  - `convex/notificationScheduler.ts`
  - small frontend polish in `src/components/AIChatPanel.tsx`, `src/contexts/AuthContext.tsx`, `src/components/RoleGuard.tsx`, `src/pages/admin/BiometricsSettings.tsx`
- Current backend scan shows remaining explicit `any`/loose env patterns are mostly:
  - webhook payload validator in `convex/admin.ts`
  - typed env access helper in `convex/lib/env.ts`
  - intentional schema payload fields in `convex/schema.ts` (`v.any()` for flexible config/audit payloads)
- Generated Convex files under `convex/_generated/` should not be edited manually.

Constraints

- Use `$team` via `omx team ...`, not spawn-agent fanout.
- Do not shut down the team until `pending=0`, `in_progress=0`, `failed=0`.
- Avoid unrelated files in the dirty worktree.
- Treat schema-level `v.any()` in `convex/schema.ts` as likely intentional unless a worker finds a safe typed replacement.

Unknowns / open questions

- Whether the remaining focused eslint surface is entirely in `convex/admin.ts` or if frontend hook/typing warnings still require small cleanup.
- Whether `convex/lib/env.ts` should keep a typed `globalThis.process?.env` shim or be refactored again.

Likely codebase touchpoints

- `convex/admin.ts`
- `convex/lib/env.ts`
- `convex/auth.config.ts`
- `convex/absenceNotifications.ts`
- `convex/notificationScheduler.ts`
- `convex/schema.ts`
- `src/components/AIChatPanel.tsx`
- `src/contexts/AuthContext.tsx`
- `src/components/RoleGuard.tsx`
- `src/pages/admin/BiometricsSettings.tsx`

Suggested worker split

- Worker 1: backend env + scheduler/auth typing cleanup (`convex/lib/env.ts`, `convex/auth.config.ts`, `convex/absenceNotifications.ts`, `convex/notificationScheduler.ts`)
- Worker 2: main backend type cleanup in `convex/admin.ts` and any safe related helpers
- Worker 3: frontend type-quality cleanup in `src/components/AIChatPanel.tsx`, `src/contexts/AuthContext.tsx`, `src/components/RoleGuard.tsx`, `src/pages/admin/BiometricsSettings.tsx`

# Context Snapshot — Wave 2 execution continuation

## Task statement
Proceed with `$team` to execute the remaining Wave 2 verification and closeout work for `leaveflow-pro`.

## Desired outcome
Finish the remaining Wave 2 verification work with team execution, keeping scope limited to:
- targeted Wave 2 test completion
- production build completion or concrete build bottleneck evidence
- exact follow-up around Convex codegen auth if it still blocks generated typings refresh

## Known facts / evidence
- Repo-local Ralph state is active and points to Wave 2.
- Wave 2 planning artifacts already exist:
  - `.omx/plans/prd-wave2-reviews-training-certifications.md`
  - `.omx/plans/test-spec-wave2-reviews-training-certifications.md`
- Fresh verification already established:
  - `npx tsc --noEmit` passes
  - targeted eslint passes
  - TS diagnostics on the Wave 2 hub and panels are clean
- `src/pages/admin/HROperationsHub.test.tsx` passes when run with:
  - `node --trace-warnings ./node_modules/vitest/vitest.mjs run ... --environment=jsdom --pool=vmThreads --maxWorkers=1`
- `src/components/reviews/PerformanceReviewsPanel.test.tsx` passes under the same invocation.
- `src/components/training/TrainingCenterPanel.test.tsx` passes under the same invocation.
- `src/components/training/CertificationsPanel.test.tsx` was simplified to avoid the Radix Select interaction path and now covers dialog entry rather than full submit flow.
- `timeout 300s npm run build` still times out without success.
- `npx convex codegen` still fails with `401 MissingAccessToken`.

## Constraints
- Repo AGENTS forbids git worktrees.
- Keep the work bounded to Wave 2 verification and closeout.
- Preserve existing Wave 1 and HR Operations behavior.
- Do not revert unrelated dirty changes.

## Unknowns / open questions
- Whether the production build is simply slow on this machine or hanging on a specific transform/plugin step.
- Whether Convex auth is available in this environment for codegen refresh during this pass.

## Likely codebase touchpoints
- `src/components/training/CertificationsPanel.test.tsx`
- `src/pages/admin/HROperationsHub.test.tsx`
- `src/components/reviews/PerformanceReviewsPanel.test.tsx`
- `src/components/training/TrainingCenterPanel.test.tsx`
- `vitest.config.ts`
- `package.json`
- `convex/_generated/*`
- `src/lib/wave2Api.ts`

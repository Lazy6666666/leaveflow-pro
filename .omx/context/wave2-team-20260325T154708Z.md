# Context Snapshot — Wave 2 team continuation

## Task statement
Continue the existing `$team $ralph` roadmap execution for `leaveflow-pro` by advancing from verified Wave 1 into Wave 2: performance reviews, training, and certifications.

## Desired outcome
Finish the remaining Wave 2 work with team execution and Ralph verification, keeping the scope bounded to the approved Wave 2 PRD and test spec.

## Known facts / evidence
- Active repo-local Ralph state already exists at `.omx/state/ralph-state.json`.
- Wave 1 is already marked verified complete in repo-local Ralph/autopilot state.
- Wave 2 planning artifacts already exist:
  - `.omx/plans/prd-wave2-reviews-training-certifications.md`
  - `.omx/plans/test-spec-wave2-reviews-training-certifications.md`
- Wave 2 implementation surface already exists locally:
  - backend modules: `convex/performanceReviews.ts`, `convex/training.ts`
  - schema additions in `convex/schema.ts`
  - UI panels under `src/components/reviews/*` and `src/components/training/*`
  - HR Operations hub tabs in `src/pages/admin/HROperationsHub.tsx`
- Fresh verification gathered during continuation:
  - `npx tsc --noEmit` passes
  - targeted eslint on Wave 2 files passes
- Concrete remaining issue discovered:
  - `convex/_generated/api.d.ts` / `convex/_generated/api.js` do not yet expose `performanceReviews` or `training`, so the frontend currently relies on `src/lib/wave2Api.ts` casting and runtime integration is incomplete until Convex codegen is refreshed
- Additional verification problem discovered:
  - targeted Wave 2 `vitest` invocations currently hang before producing per-test results, indicating a shared test-runtime/setup issue rather than an isolated assertion failure

## Constraints
- Repo AGENTS forbids git worktrees for this repository.
- Wave 2 scope must stay bounded to reviews, training, and certifications.
- Schema changes must remain additive only.
- Preserve existing HR Operations shell and Wave 1 tabs/routes.
- Team execution must avoid violating the repo no-worktree rule.

## Unknowns / open questions
- Whether `omx team` launched from the parent workspace can continue without creating repo worktrees while workers edit `leaveflow-pro` directly.
- Whether the stuck Vitest lane is caused by jsdom/runtime harness issues or a newly introduced component interaction loop.

## Likely codebase touchpoints
- `convex/_generated/*`
- `convex/performanceReviews.ts`
- `convex/training.ts`
- `convex/schema.ts`
- `src/lib/wave2Api.ts`
- `src/pages/admin/HROperationsHub.tsx`
- `src/pages/admin/HROperationsHub.test.tsx`
- `src/components/reviews/*`
- `src/components/training/*`

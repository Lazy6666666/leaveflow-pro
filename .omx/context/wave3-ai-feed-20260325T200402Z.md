# Context Snapshot — Wave 3 AI feed slice

## Task statement
Continue the remaining Wave 3 roadmap by implementing the AI suggestions and action feed slice after expenses and policy/integrations.

## Desired outcome
Deliver a bounded AI feed surface that uses the existing assistant infrastructure without bypassing role-aware visibility, and keep the implementation scoped to Wave 3.

## Known facts / evidence
- Wave 3 PRD and test spec already exist and explicitly reserve the remaining slice for AI feed work.
- The expenses slice is already present in the repo.
- The policy acknowledgements + integrations slice is already present in the repo.
- Existing assistant infrastructure already exists under:
  - `convex/assistant.ts`
  - `convex/assistantReplies.ts`
  - `convex/assistantUtils.ts`
  - `src/components/AIChatPanel.tsx`
  - `src/components/ai-chat/*`
  - `src/pages/AIWorkspace.tsx`
- No dedicated Wave 3 AI feed files exist yet.

## Constraints
- Keep the feed inside existing permission boundaries.
- Prefer additive schema only if persistence is truly needed.
- Do not redesign the assistant architecture.
- Preserve Waves 1-2 and the existing Wave 3 slices.

## Likely touchpoints
- `convex/assistant.ts`
- `convex/assistantReplies.ts`
- `convex/assistantUtils.ts`
- `src/components/ai-chat/*`
- `src/pages/AIWorkspace.tsx`
- `src/components/ai-feed/*`
- optional `convex/aiActionFeed.ts`
